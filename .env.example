# Passcode for the /admin dashboard
ADMIN_PASSCODE=trovert2025

# Extra salt for the admin session cookie
ADMIN_SECRET=change-me-to-a-long-random-string

# Not used yet — the app runs on the JSON store in .data/db.json.
# When the real database lands, add an adapter in src/lib/db/ and read this.
# DATABASE_URL=
